---
name: 15-labor
title: 15-劳动争议纠纷-民事起诉状 — 要素定稿（精调级）
case_type: 15-labor
template_tree: 15-劳动争议纠纷-民事起诉状
version: "1.0"
status: stable
generated: 2026-08-17 由 15-labor-sample.json + fill_template 规则反推
---

# 15-劳动争议纠纷-民事起诉状（精调级定稿）

> 模板树 `templates/15-劳动争议纠纷-民事起诉状/`；规则 `build_rules_*`（叠加模式：案由特定在前 + 通用层在后）。
> 通用块（当事人 自然人N/法人N、编号标题填空、唯一标签、调解、具状、**通用勾选**）见 `../common-elements.md` 与骨架 `../skeletons/15-劳动争议纠纷-民事起诉状.md`，此处只列案由特定增量。

## 一、案由特定字段（诉讼请求/事实）

| elements 路径 | 类型 | 渲染方式 |
|---|---|---|
| `工资支付…违法解除赔偿金 ×7` | {勾选,明细} | 标题锚定位「是□ 否□ 明细：」；键=工资支付/双倍工资/加班费/未休年休假工资/社保经济损失/解除经济补偿/违法解除赔偿金 |
| `是否主张诉讼费用` | bool | 纯是/否段 |

## 二、通用勾选（本案由大勾选群走此机制）

elements 顶层 `勾选` 键：`{"锚文本": "选项原文"}`（多选用列表）。示例：

```json
"勾选": {"是否主张工资支付": "是"}
```

锚=骨架"段落原文"列任一稳定子串；选项=骨架"选项/标签"列原文。

## 三、夹具与回归

- 样例：`tests/fixtures/15-labor-sample.json`；断言见 `tests/run_e2e.sh` CHECKS["tests/output/15-labor.docx"]
