---
name: 24-patent
title: 24-侵害发明专利权纠纷-民事起诉状 — 要素定稿（精调级）
case_type: 24-patent
template_tree: 24-侵害发明专利权纠纷-民事起诉状
version: "1.0"
status: stable
generated: 2026-08-17 由 24-patent-sample.json + fill_template 规则反推
---

# 24-侵害发明专利权纠纷-民事起诉状（精调级定稿）

> 模板树 `templates/24-侵害发明专利权纠纷-民事起诉状/`；规则 `build_rules_*`（叠加模式：案由特定在前 + 通用层在后）。
> 通用块（当事人 自然人N/法人N、编号标题填空、唯一标签、调解、具状、**通用勾选**）见 `../common-elements.md` 与骨架 `../skeletons/24-侵害发明专利权纠纷-民事起诉状.md`，此处只列案由特定增量。

## 一、案由特定字段（诉讼请求/事实）

| elements 路径 | 类型 | 渲染方式 |
|---|---|---|
| `停止侵权` | {勾选,内容} | 两段式有/无 |
| `惩罚性赔偿` | {勾选,基数,倍数} | 整段重写『包含☑ 基数 元×（1+ 倍数）』 |
| `费用行` | — | 模板为费名独立段+凭证独立段，金额人工/后续精调；凭证勾选走通用勾选 |

## 二、通用勾选（本案由大勾选群走此机制）

elements 顶层 `勾选` 键：`{"锚文本": "选项原文"}`（多选用列表）。示例：

```json
"勾选": {"包含": "包含"}
```

锚=骨架"段落原文"列任一稳定子串；选项=骨架"选项/标签"列原文。

## 三、夹具与回归

- 样例：`tests/fixtures/24-patent-sample.json`；断言见 `tests/run_e2e.sh` CHECKS["tests/output/24-patent.docx"]
