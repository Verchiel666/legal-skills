---
name: 22-copyright
title: 22-侵害著作权及邻接权纠纷-民事起诉状 — 要素定稿（精调级）
case_type: 22-copyright
template_tree: 22-侵害著作权及邻接权纠纷-民事起诉状
version: "1.0"
status: stable
generated: 2026-08-17 由 22-copyright-sample.json + fill_template 规则反推
---

# 22-侵害著作权及邻接权纠纷-民事起诉状（精调级定稿）

> 模板树 `templates/22-侵害著作权及邻接权纠纷-民事起诉状/`；规则 `build_rules_*`（叠加模式：案由特定在前 + 通用层在后）。
> 通用块（当事人 自然人N/法人N、编号标题填空、唯一标签、调解、具状、**通用勾选**）见 `../common-elements.md` 与骨架 `../skeletons/22-侵害著作权及邻接权纠纷-民事起诉状.md`，此处只列案由特定增量。

## 一、案由特定字段（诉讼请求/事实）

| elements 路径 | 类型 | 渲染方式 |
|---|---|---|
| `诉讼请求.完整表述` | list[str] | 说明段后整段插入（每项一段，正文默认字体） |
| `事实与理由.完整表述` | list[str] | 同上 |
| `经济损失` | str | 金额句 |
| `律师费/取证费/差旅费` | {金额,凭证} | 费用行（结构保持·布局无关：金额填空白+凭证勾选） |
| `侵权链接/计算依据或参考因素` | str | 标签追加 |
| `停止侵权` | {勾选,作品名称,登记证号} | 前置框☑+占位括号填值（v0.14） |
| `其他请求` | str | 官方指引文本后追加（v0.14） |
| `事实与理由.著作权客体` | {作品名称,作品完成时间,首次发表,登记时间} | 标签追加，日期 ISO（v0.14） |
| `事实与理由.侵权时间地点` | str | 标签后空段写入（v0.14） |
| `事实与理由.权项.信息网络传播权/侵权通知` | bool / "否" | 尾随框☑ / 前置框☑（v0.14） |
| `事实与理由.行为方式.其他` | bool | "□ 12."前置框☑（v0.14） |
| `关联案件` | "无" | 前段"无□"→"无☑"（v0.14） |
| `权属/作品性质/侵权种类等大勾选群` | — | 走通用勾选（见下） |

## 二、通用勾选（本案由大勾选群走此机制）

elements 顶层 `勾选` 键：`{"锚文本": "选项原文"}`（多选用列表）。示例：

```json
"勾选": {"合作作品": "合作作品", "原告损失": "原告损失"}
```

锚=骨架"段落原文"列任一稳定子串；选项=骨架"选项/标签"列原文。

## 三、夹具与回归

- 样例：`tests/fixtures/22-copyright-sample.json`；断言见 `tests/run_e2e.sh` CHECKS["tests/output/22-copyright.docx"]
